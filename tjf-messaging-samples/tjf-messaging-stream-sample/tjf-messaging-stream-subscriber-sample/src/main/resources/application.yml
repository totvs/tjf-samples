server:
  port: 8180

spring:
  cloud:
    #function:
    #  definition: subscribeArrived
    stream:
      binders:
        rabbit1:
          type: rabbit
      bindings:
        starship-input:
          binder: rabbit1
          destination: starship-input
          group: reader
           
      rabbit:
        default:
          exchangeType: headers
        bindings:
          starship-input:
            consumer:
              exchangeType: headers
            producer:
              exchangeType: headers
